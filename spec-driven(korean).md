# 사양 주도 개발 (Specification-Driven Development, SDD)

## 권력의 역전 (The Power Inversion)

수십 년 동안 코드는 왕이었습니다. 사양(Spec)은 코드를 섬기는 존재였으며, 우리가 "진짜 작업(코딩)"을 시작하면 버려지는 비계(scaffolding)에 불과했습니다. 우리는 개발을 안내하기 위해 PRD(제품 요구 사항 문서)를 썼고, 구현을 알리기 위해 설계 문서를 작성했으며, 아키텍처를 시각화하기 위해 다이어그램을 그렸습니다. 하지만 이 모든 것은 항상 코드 자체에 종속되었습니다. 코드가 진실이었습니다. 그 외의 모든 것은 기껏해야 좋은 의도일 뿐이었습니다. 코드는 진실의 원천(Source of Truth)이었고, 개발이 진행됨에 따라 사양은 그 속도를 따라가기 힘들었습니다. 자산(코드)과 구현이 하나이기 때문에, 코드를 기반으로 하지 않고 병렬 구현을 갖는 것은 쉽지 않았습니다.

**사양 주도 개발(SDD)**은 이러한 권력 구조를 뒤집습니다. 사양이 코드를 섬기는 것이 아니라, **코드가 사양을 섬깁니다.** PRD는 구현을 위한 가이드가 아니라, 구현을 생성하는 원천입니다. 기술 계획은 코딩에 정보를 주는 문서가 아니라, 코드를 생산하는 정밀한 정의입니다. 이것은 소프트웨어를 만드는 방식의 점진적인 개선이 아닙니다. 이것은 개발을 주도하는 것이 무엇인지에 대한 근본적인 재검토입니다.

사양과 구현 사이의 격차는 소프트웨어 개발 초기부터 우리를 괴롭혀 왔습니다. 우리는 더 나은 문서화, 더 상세한 요구 사항, 더 엄격한 프로세스로 이 격차를 메우려 노력했습니다. 이러한 접근 방식이 실패한 이유는 격차를 불가피한 것으로 받아들였기 때문입니다. 그들은 격차를 좁히려고만 했지 제거하려고 하지 않았습니다. SDD는 사양과 사양에서 파생된 구체적인 구현 계획을 **실행 가능(executable)**하게 만듦으로써 격차를 없앱니다. 사양과 구현 계획이 코드를 생성할 때, 격차는 존재하지 않으며 오직 변환(transformation)만이 존재합니다.

이러한 변환은 이제 AI가 복잡한 사양을 이해하고 구현하고 상세한 구현 계획을 생성할 수 있게 됨에 따라 가능해졌습니다. 하지만 구조 없는 순수한 AI 생성은 혼란을 야기합니다. SDD는 작동하는 시스템을 생성할 수 있을 만큼 정밀하고, 완전하며, 모호하지 않은 사양과 그에 따른 구현 계획을 통해 구조를 제공합니다. 사양은 주요 산출물이 됩니다. 코드는 특정 언어와 프레임워크로 표현된 사양의 표현(구현 계획에 따른 구현)이 됩니다.

이 새로운 세상에서 소프트웨어를 유지보수한다는 것은 사양을 발전시킨다는 것을 의미합니다. 개발 팀의 의도는 자연어("의도 주도 개발"), 디자인 자산, 핵심 원칙 및 기타 가이드라인으로 표현됩니다. 개발의 공용어(lingua franca)는 더 높은 수준으로 이동하며, 코드는 라스트 마일(last-mile) 접근 방식이 됩니다.

디버깅은 잘못된 코드를 생성하는 사양과 구현 계획을 수정하는 것을 의미합니다. 리팩터링은 명확성을 위해 구조를 재조정하는 것을 의미합니다. 전체 개발 워크플로우는 사양을 진실의 중심 원천으로 삼고, 구현 계획과 코드를 지속적으로 재생성되는 산출물로 재편합니다. 새로운 기능으로 앱을 업데이트하거나 창의적인 이유로 새로운 병렬 구현을 만드는 것은 사양을 다시 검토하고 새로운 구현 계획을 만드는 것을 의미합니다. 따라서 이 프로세스는 0 -> 1, (1', ..), 2, 3, N으로 진행됩니다.

개발 팀은 창의성, 실험, 비판적 사고에 집중하게 됩니다.

## 실전에서의 SDD 워크플로우

워크플로우는 종종 모호하고 불완전한 아이디어에서 시작됩니다. AI와의 반복적인 대화를 통해 이 아이디어는 포괄적인 PRD가 됩니다. AI는 명확히 하는 질문을 던지고, 엣지 케이스를 식별하며, 정밀한 인수 조건(Acceptance Criteria)을 정의하도록 돕습니다. 전통적인 개발에서 며칠 간의 회의와 문서화가 필요했던 작업이 몇 시간의 집중적인 사양 작업으로 완료됩니다. 이는 전통적인 SDLC(소프트웨어 개발 생명주기)를 변환시킵니다. 요구 사항과 설계는 분리된 단계가 아니라 지속적인 활동이 됩니다. 이는 팀 검토를 거친 사양이 표현되고 버전 관리되며, 브랜치에서 생성되고 병합되는 팀 프로세스를 지원합니다.

제품 관리자(PM)가 인수 조건을 업데이트하면, 구현 계획은 영향을 받는 기술적 결정사항을 자동으로 표시합니다. 아키텍트가 더 나은 패턴을 발견하면, PRD는 새로운 가능성을 반영하여 업데이트됩니다.

이 사양 과정 전반에 걸쳐, **조사 에이전트(Research Agents)**들이 중요한 맥락을 수집합니다. 그들은 라이브러리 호환성, 성능 벤치마크, 보안 영향을 조사합니다. 조직의 제약 조건이 자동으로 발견되고 적용됩니다. 회사의 데이터베이스 표준, 인증 요구 사항, 배포 정책이 모든 사양에 매끄럽게 통합됩니다.

PRD로부터 AI는 요구 사항을 기술적 결정으로 매핑하는 구현 계획을 생성합니다. 모든 기술 선택에는 문서화된 근거가 있습니다. 모든 아키텍처 결정은 특정 요구 사항으로 거슬러 올라갑니다. 이 과정 전반에 걸쳐 **일관성 검증(Consistency Validation)**이 지속적으로 품질을 향상시킵니다. AI는 모호함, 모순, 공백을 찾기 위해 사양을 분석합니다. 이는 일회성 관문이 아니라 지속적인 정제 과정입니다.

코드 생성은 사양과 구현 계획이 충분히 안정되면 즉시 시작되지만, 반드시 "완전할" 필요는 없습니다. 초기 생성은 탐색적일 수 있으며, 사양이 실제로 말이 되는지 테스트하는 용도일 수 있습니다. 도메인 개념은 데이터 모델이 됩니다. 사용자 스토리는 API 엔드포인트가 됩니다. 인수 시나리오는 테스트가 됩니다. 이것은 사양을 통해 개발과 테스트를 병합합니다. 테스트 시나리오는 코드 작성 후에 쓰여지는 것이 아니라, 구현과 테스트를 모두 생성하는 사양의 일부입니다.

피드백 루프는 초기 개발 너머로 확장됩니다. 프로덕션 지표와 사건(incident)들은 단순히 핫픽스를 유발하는 것이 아니라, 다음 재생성을 위해 사양을 업데이트합니다. 성능 병목 현상은 새로운 비기능 요구 사항이 됩니다. 보안 취약점은 모든 미래의 생성에 영향을 미치는 제약 조건이 됩니다. 사양, 구현, 운영 현실 사이의 이러한 반복적인 춤은 진정한 이해가 나타나는 곳이며, 전통적인 SDLC가 지속적인 진화로 변모하는 지점입니다.

## 왜 지금 SDD가 중요한가

세 가지 트렌드가 SDD를 가능하게 할 뿐만 아니라 필수적인 것으로 만들고 있습니다.

1. **AI 역량의 임계값 도달**: 자연어 사양이 작동하는 코드를 신뢰성 있게 생성할 수 있는 수준에 도달했습니다. 이것은 개발자를 대체하는 것이 아니라, 사양에서 구현으로의 기계적인 번역을 자동화함으로써 그들의 효율성을 증폭시키는 것입니다. 이는 탐색과 창의성을 증폭시키고, "다시 시작하기"를 쉽게 지원하며, 추가, 삭제 및 비판적 사고를 지원할 수 있습니다.
2. **소프트웨어 복잡성의 증가**: 현대 시스템은 수십 개의 서비스, 프레임워크, 의존성을 통합합니다. 수동 프로세스를 통해 이 모든 조각을 원래의 의도와 일치시키는 것은 점점 더 어려워지고 있습니다. SDD는 사양 주도 생성을 통해 체계적인 정렬을 제공합니다. 프레임워크는 인간 우선 지원이 아닌 AI 우선 지원을 제공하거나 재사용 가능한 컴포넌트를 중심으로 설계되는 방향으로 진화할 수 있습니다.
3. **변화 속도의 가속화**: 요구 사항은 그 어느 때보다 빠르게 변화합니다. 피벗(Pivot)은 더 이상 예외가 아니라 예상되는 일입니다. 현대의 제품 개발은 사용자 피드백, 시장 상황, 경쟁 압력에 기반한 빠른 반복을 요구합니다. 전통적인 개발은 이러한 변화를 방해 요소로 취급합니다. 각 피벗은 문서, 설계, 코드 전반에 걸쳐 수동으로 변경 사항을 전파해야 합니다. 그 결과는 속도를 제한하는 느리고 신중한 업데이트이거나, 기술 부채를 쌓는 빠르고 무모한 변경입니다.

SDD는 가상 시뮬레이션 실험을 지원할 수 있습니다: *"티셔츠를 더 많이 팔려는 비즈니스 니즈를 위해 애플리케이션을 재구현하거나 변경해야 한다면, 어떻게 구현하고 실험할 것인가?"*

SDD는 요구 사항 변경을 장애물에서 일반적인 워크플로우로 전환합니다. 사양이 구현을 주도할 때, 피벗은 수동 재작성이 아니라 체계적인 재생성이 됩니다. PRD에서 핵심 요구 사항을 변경하면 영향을 받는 구현 계획이 자동으로 업데이트됩니다. 사용자 스토리를 수정하면 해당 API 엔드포인트가 재생성됩니다. 이것은 단순히 초기 개발에 관한 것이 아니라, 필연적인 변화 속에서도 엔지니어링 속도를 유지하는 것에 관한 것입니다.

## 핵심 원칙 (Core Principles)

* **공용어로서의 사양 (Specifications as the Lingua Franca)**: 사양이 주요 산출물이 됩니다. 코드는 특정 언어와 프레임워크에서의 표현이 됩니다. 소프트웨어 유지보수는 사양을 발전시키는 것을 의미합니다.
* **실행 가능한 사양 (Executable Specifications)**: 사양은 작동하는 시스템을 생성할 수 있을 만큼 정밀하고, 완전하며, 모호하지 않아야 합니다. 이는 의도와 구현 사이의 격차를 없앱니다.
* **지속적인 정제 (Continuous Refinement)**: 일관성 검증은 일회성 관문이 아니라 지속적으로 발생합니다. AI는 지속적인 프로세스로서 사양의 모호함, 모순, 공백을 분석합니다.
* **조사 주도 맥락 (Research-Driven Context)**: 조사 에이전트는 사양 과정 전반에 걸쳐 기술적 옵션, 성능 영향, 조직적 제약 조건을 조사하여 중요한 맥락을 수집합니다.
* **양방향 피드백 (Bidirectional Feedback)**: 프로덕션 현실이 사양의 진화에 정보를 줍니다. 지표, 사건, 운영상의 배움이 사양 정제를 위한 입력값이 됩니다.
* **탐색을 위한 브랜칭 (Branching for Exploration)**: 동일한 사양에서 여러 구현 접근 방식을 생성하여 성능, 유지보수성, 사용자 경험, 비용 등 서로 다른 최적화 목표를 탐색합니다.

## 구현 접근 방식 (Implementation Approaches)

오늘날 SDD를 실천하려면 기존 도구들을 조합하고 프로세스 전반에 걸쳐 규율을 유지해야 합니다. 이 방법론은 다음과 같이 실천할 수 있습니다.

* 반복적인 사양 개발을 위한 AI 어시스턴트
* 기술적 맥락 수집을 위한 조사 에이전트
* 사양을 구현으로 번역하는 코드 생성 도구
* 사양 우선(specification-first) 워크플로우에 맞게 조정된 버전 관리 시스템
* 사양 문서의 AI 분석을 통한 일관성 확인

핵심은 사양을 진실의 원천으로 취급하고, 코드는 사양을 섬기는 생성된 산출물로 취급하는 것입니다.

## 명령어를 통한 SDD 간소화

SDD 방법론은 사양 → 계획 → 작업(tasking) 워크플로우를 자동화하는 세 가지 강력한 명령어를 통해 크게 향상됩니다.

### `/speckit.specify` 명령어

이 명령어는 단순한 기능 설명(사용자 프롬프트)을 자동 저장소 관리가 포함된 완전하고 구조화된 사양으로 변환합니다.

1. **자동 기능 번호 지정**: 기존 사양을 스캔하여 다음 기능 번호를 결정 (예: 001, 002, 003)
2. **브랜치 생성**: 설명에서 의미론적 브랜치 이름을 생성하고 자동으로 생성
3. **템플릿 기반 생성**: 기능 사양 템플릿을 복사하고 요구 사항에 맞게 커스터마이징
4. **디렉토리 구조**: 모든 관련 문서에 대해 적절한 `specs/[branch-name]/` 구조 생성

### `/speckit.plan` 명령어

기능 사양이 존재하면, 이 명령어는 포괄적인 구현 계획을 생성합니다:

1. **사양 분석**: 기능 요구 사항, 사용자 스토리, 인수 조건을 읽고 이해
2. **헌법 준수**: 프로젝트 헌법 및 아키텍처 원칙과의 정렬 보장
3. **기술적 번역**: 비즈니스 요구 사항을 기술 아키텍처 및 구현 세부 사항으로 변환
4. **상세 문서화**: 데이터 모델, API 계약, 테스트 시나리오에 대한 지원 문서 생성
5. **퀵스타트 검증**: 핵심 검증 시나리오를 캡처하는 퀵스타트 가이드 생성

### `/speckit.tasks` 명령어

계획이 생성된 후, 이 명령어는 계획과 관련 설계 문서를 분석하여 실행 가능한 작업 목록을 생성합니다:

1. **입력**: `plan.md`(필수)와 존재하는 경우 `data-model.md`, `contracts/`, `research.md`를 읽음
2. **작업 도출**: 계약, 엔터티, 시나리오를 구체적인 작업으로 변환
3. **병렬화**: 독립적인 작업에 `[P]` 표시를 하고 안전한 병렬 그룹을 개요화
4. **출력**: 기능 디렉토리에 `tasks.md`를 작성하여 Task 에이전트가 실행할 준비를 완료

### 예제: 채팅 기능 구축

이 명령어들이 전통적인 개발 워크플로우를 어떻게 변환하는지 보여줍니다:

**전통적인 접근 방식:**

```text
1. 문서에 PRD 작성 (2-3시간)
2. 설계 문서 생성 (2-3시간)
3. 수동으로 프로젝트 구조 설정 (30분)
4. 기술 사양 작성 (3-4시간)
5. 테스트 계획 생성 (2시간)
총계: ~12시간의 문서화 작업
```

**명령어를 활용한 SDD 접근 방식:**

```bash
# Step 1: Generate feature specification (5 min)
/speckit.specify Real-time chat system with message history and user presence detection

# Automatically performs:
# - Creates "003-chat-system" branch
# - Creates specs/003-chat-system/spec.md
# - Populates with structured requirements

# Step 2: Generate implementation plan (5 min)
/speckit.plan WebSocket for real-time messaging, PostgreSQL for history, Redis for presence detection

# Step 3: Generate executable tasks (5 min)
/speckit.tasks

# Automatically generates:
# - specs/003-chat-system/plan.md
# - specs/003-chat-system/research.md (WebSocket library comparison)
# - specs/003-chat-system/data-model.md (Message and User schemas)
# - specs/003-chat-system/contracts/ (WebSocket events, REST endpoints)
# - specs/003-chat-system/quickstart.md (Core validation scenarios)
# - specs/003-chat-system/tasks.md (Task list derived from plan)
```

15분 만에 다음을 얻게 됩니다:

- 사용자 스토리와 인수 조건이 포함된 완전한 기능 사양
- 기술 선택과 근거가 포함된 상세한 구현 계획
- 코드 생성 준비가 완료된 API 계약과 데이터 모델
- 자동화 및 수동 테스트를 위한 포괄적인 테스트 시나리오
- 기능 브랜치에 적절히 버전 관리된 모든 문서

### 구조화된 자동화의 힘

이 명령어들은 단순히 시간을 절약하는 것이 아니라 일관성과 완전성을 강제합니다:

1. **잊힌 세부 사항 없음**: 템플릿이 비기능 요구 사항부터 오류 처리까지 모든 측면이 고려되도록 보장
2. **추적 가능한 결정**: 모든 기술 선택이 특정 요구 사항으로 연결됨
3. **살아있는 문서**: 사양이 코드를 생성하기 때문에 코드와 동기화 상태 유지
4. **빠른 반복**: 요구 사항을 변경하고 며칠이 아닌 몇 분 만에 계획 재생성

이 명령어들은 사양을 정적 문서가 아닌 실행 가능한 산출물로 취급함으로써 SDD 원칙을 구현합니다. 사양 프로세스를 필요악에서 개발을 주도하는 힘으로 변환합니다.

### 템플릿 주도 품질: 구조가 더 나은 결과를 위해 LLM을 제약하는 방법

이 명령어들의 진정한 힘은 단순한 자동화가 아니라, 템플릿이 LLM의 행동을 더 높은 품질의 사양으로 유도하는 방식에 있습니다. 템플릿은 생산적인 방식으로 LLM의 출력을 제약하는 정교한 프롬프트 역할을 합니다:

#### 1. **조급한 구현 세부 사항 방지**

기능 사양 템플릿은 다음을 명시적으로 지시합니다:

```text
- ✅ 사용자가 무엇(WHAT)을 필요로 하고 왜(WHY) 필요한지에 집중
- ❌ 어떻게(HOW) 구현할지는 피할 것 (기술 스택, API, 코드 구조 없음)
```

이 제약은 LLM이 적절한 추상화 수준을 유지하도록 강제합니다. LLM이 자연스럽게 "React와 Redux로 구현"으로 점프할 수 있을 때, 템플릿은 "사용자는 데이터의 실시간 업데이트가 필요함"에 집중하도록 유지합니다. 이러한 분리는 구현 기술이 변경되어도 사양이 안정적으로 유지되도록 보장합니다.

#### 2. **명시적 불확실성 마커 강제**

두 템플릿 모두 `[NEEDS CLARIFICATION]` 마커 사용을 필수화합니다:

```text
사용자 프롬프트에서 이 사양을 생성할 때:
1. **모든 모호함에 표시**: [NEEDS CLARIFICATION: 구체적인 질문] 사용
2. **추측하지 말 것**: 프롬프트에 명시되지 않은 것은 표시할 것
```

이는 LLM이 그럴듯하지만 잠재적으로 잘못된 가정을 하는 일반적인 행동을 방지합니다. "로그인 시스템"이 이메일/비밀번호 인증을 사용한다고 추측하는 대신, LLM은 `[NEEDS CLARIFICATION: 인증 방법이 지정되지 않음 - 이메일/비밀번호, SSO, OAuth?]`로 표시해야 합니다.

#### 3. **체크리스트를 통한 구조적 사고**

템플릿에는 사양에 대한 "단위 테스트" 역할을 하는 포괄적인 체크리스트가 포함되어 있습니다:

```markdown
### 요구 사항 완전성

- [ ] [NEEDS CLARIFICATION] 마커가 남아 있지 않음
- [ ] 요구 사항이 테스트 가능하고 모호하지 않음
- [ ] 성공 기준이 측정 가능함
```

이 체크리스트들은 LLM이 체계적으로 출력을 자체 검토하도록 강제하여, 그렇지 않으면 놓칠 수 있는 공백을 포착합니다. LLM에게 품질 보증 프레임워크를 제공하는 것과 같습니다.

#### 4. **관문을 통한 헌법 준수**

구현 계획 템플릿은 단계별 관문을 통해 아키텍처 원칙을 강제합니다:

```markdown
### Phase -1: 구현 전 관문

#### 단순성 관문 (Article VII)

- [ ] 3개 이하의 프로젝트 사용?
- [ ] 미래 대비 설계 없음?

#### 반추상화 관문 (Article VIII)

- [ ] 프레임워크를 직접 사용?
- [ ] 단일 모델 표현?
```

이 관문들은 LLM이 모든 복잡성을 명시적으로 정당화하도록 함으로써 과잉 엔지니어링을 방지합니다. 관문이 실패하면 LLM은 "복잡성 추적" 섹션에 이유를 문서화해야 하며, 아키텍처 결정에 대한 책임성을 생성합니다.

#### 5. **계층적 세부 사항 관리**

템플릿은 적절한 정보 아키텍처를 강제합니다:

```text
**중요**: 이 구현 계획은 고수준이고 읽기 쉽게 유지되어야 합니다.
코드 샘플, 상세한 알고리즘, 또는 광범위한 기술 사양은
적절한 `implementation-details/` 파일에 배치해야 합니다.
```

이는 사양이 읽을 수 없는 코드 덤프가 되는 일반적인 문제를 방지합니다. LLM은 복잡성을 별도 파일로 추출하면서 주요 문서의 탐색 가능성을 유지하는 적절한 세부 수준을 학습합니다.

#### 6. **테스트 우선 사고**

구현 템플릿은 테스트 우선 개발을 강제합니다:

```text
### 파일 생성 순서
1. API 사양과 함께 `contracts/` 생성
2. 순서대로 테스트 파일 생성: 계약 → 통합 → e2e → 단위
3. 테스트를 통과시키기 위한 소스 파일 생성
```

이 순서 제약은 LLM이 구현 전에 테스트 가능성과 계약에 대해 생각하도록 보장하여, 더 견고하고 검증 가능한 사양으로 이어집니다.

#### 7. **투기적 기능 방지**

템플릿은 명시적으로 추측을 억제합니다:

```text
- [ ] 투기적이거나 "필요할 수도 있는" 기능 없음
- [ ] 모든 단계에 명확한 전제 조건과 산출물이 있음
```

이는 LLM이 구현을 복잡하게 만드는 "있으면 좋은" 기능을 추가하는 것을 막습니다. 모든 기능은 명확한 인수 조건이 있는 구체적인 사용자 스토리로 추적되어야 합니다.

### 복합 효과

이러한 제약들은 함께 작용하여 다음과 같은 사양을 생성합니다:

- **완전함**: 체크리스트가 아무것도 잊혀지지 않도록 보장
- **모호하지 않음**: 강제된 명확화 마커가 불확실성을 강조
- **테스트 가능함**: 테스트 우선 사고가 프로세스에 내장됨
- **유지보수 가능함**: 적절한 추상화 수준과 정보 계층
- **구현 가능함**: 구체적인 산출물이 있는 명확한 단계

템플릿은 LLM을 창의적인 작가에서 규율 있는 사양 엔지니어로 변환하여, 그 능력을 진정으로 개발을 주도하는 일관되게 고품질의 실행 가능한 사양을 생산하는 방향으로 유도합니다.

## 헌법적 기반: 아키텍처 규율 강제

SDD의 핵심에는 헌법(Constitution)이 있습니다. 이는 사양이 코드가 되는 방식을 지배하는 불변의 원칙 집합입니다. 헌법(`memory/constitution.md`)은 시스템의 아키텍처 DNA 역할을 하여, 모든 생성된 구현이 일관성, 단순성, 품질을 유지하도록 보장합니다.

### 개발의 9개 조항

헌법은 개발 프로세스의 모든 측면을 형성하는 9개 조항을 정의합니다:

#### 제1조: 라이브러리 우선 원칙

모든 기능은 예외 없이 독립형 라이브러리로 시작해야 합니다. 이는 처음부터 모듈식 설계를 강제합니다:

```text
Specify의 모든 기능은 반드시 독립형 라이브러리로 존재를 시작해야 합니다.
어떤 기능도 먼저 재사용 가능한 라이브러리 컴포넌트로 추상화되지 않고
애플리케이션 코드 내에 직접 구현되어서는 안 됩니다.
```

이 원칙은 사양이 모놀리식 애플리케이션이 아닌 모듈식의 재사용 가능한 코드를 생성하도록 보장합니다. LLM이 구현 계획을 생성할 때, 명확한 경계와 최소한의 의존성을 가진 라이브러리로 기능을 구조화해야 합니다.

#### 제2조: CLI 인터페이스 의무

모든 라이브러리는 명령줄 인터페이스를 통해 기능을 노출해야 합니다:

```text
모든 CLI 인터페이스는 반드시:
- 텍스트를 입력으로 받아야 함 (stdin, 인수, 또는 파일을 통해)
- 텍스트를 출력으로 생성해야 함 (stdout을 통해)
- 구조화된 데이터 교환을 위한 JSON 형식 지원
```

이는 관찰 가능성과 테스트 가능성을 강제합니다. LLM은 불투명한 클래스 내부에 기능을 숨길 수 없습니다. 모든 것이 텍스트 기반 인터페이스를 통해 접근 가능하고 검증 가능해야 합니다.

#### 제3조: 테스트 우선 명령

가장 변혁적인 조항입니다. 테스트 전에는 코드 없음:

```text
이것은 협상 불가입니다: 모든 구현은 반드시 엄격한 테스트 주도 개발을 따라야 합니다.
다음 조건이 충족되기 전에는 어떤 구현 코드도 작성되어서는 안 됩니다:
1. 단위 테스트가 작성됨
2. 테스트가 사용자에 의해 검증되고 승인됨
3. 테스트가 실패함을 확인 (Red 단계)
```

이는 전통적인 AI 코드 생성을 완전히 뒤집습니다. 코드를 생성하고 작동하기를 바라는 대신, LLM은 먼저 동작을 정의하는 포괄적인 테스트를 생성하고, 승인을 받은 다음에만 구현을 생성해야 합니다.

#### 제7조 & 제8조: 단순성과 반추상화

이 쌍을 이루는 조항들은 과잉 엔지니어링에 대항합니다:

```text
섹션 7.3: 최소 프로젝트 구조
- 초기 구현에 최대 3개 프로젝트
- 추가 프로젝트는 문서화된 정당성 필요

섹션 8.1: 프레임워크 신뢰
- 프레임워크 기능을 래핑하지 않고 직접 사용
```

LLM이 자연스럽게 정교한 추상화를 만들 수 있을 때, 이 조항들은 모든 복잡성 계층을 정당화하도록 강제합니다. 구현 계획 템플릿의 "Phase -1 관문"은 이러한 원칙을 직접 강제합니다.

#### 제9조: 통합 우선 테스트

격리된 단위 테스트보다 실제 환경 테스트를 우선시합니다:

```text
테스트는 반드시 현실적인 환경을 사용해야 합니다:
- 모의 객체(mock)보다 실제 데이터베이스 선호
- 스텁(stub)보다 실제 서비스 인스턴스 사용
- 구현 전 계약 테스트 필수
```

이는 생성된 코드가 이론상이 아닌 실제로 작동하도록 보장합니다.

### 템플릿을 통한 헌법 집행

구현 계획 템플릿은 구체적인 체크포인트를 통해 이러한 조항들을 운용화합니다:

```markdown
### Phase -1: 구현 전 관문

#### 단순성 관문 (제7조)

- [ ] 3개 이하의 프로젝트 사용?
- [ ] 미래 대비 설계 없음?

#### 반추상화 관문 (제8조)

- [ ] 프레임워크를 직접 사용?
- [ ] 단일 모델 표현?

#### 통합 우선 관문 (제9조)

- [ ] 계약 정의됨?
- [ ] 계약 테스트 작성됨?
```

이 관문들은 아키텍처 원칙에 대한 컴파일 타임 검사 역할을 합니다. LLM은 관문을 통과하거나 "복잡성 추적" 섹션에 정당화된 예외를 문서화하지 않고는 진행할 수 없습니다.

### 불변 원칙의 힘

헌법의 힘은 그 불변성에 있습니다. 구현 세부 사항은 진화할 수 있지만, 핵심 원칙은 일정하게 유지됩니다. 이는 다음을 제공합니다:

1. **시간에 걸친 일관성**: 오늘 생성된 코드는 내년에 생성될 코드와 동일한 원칙을 따름
2. **LLM 간 일관성**: 서로 다른 AI 모델이 아키텍처적으로 호환되는 코드를 생성
3. **아키텍처 무결성**: 모든 기능이 시스템 설계를 약화시키는 것이 아니라 강화
4. **품질 보증**: 테스트 우선, 라이브러리 우선, 단순성 원칙이 유지보수 가능한 코드를 보장

### 헌법의 진화

원칙은 불변이지만, 그 적용은 진화할 수 있습니다:

```text
섹션 4.2: 수정 절차
이 헌법의 수정은 다음을 요구합니다:
- 변경 근거의 명시적 문서화
- 프로젝트 유지관리자의 검토 및 승인
- 역호환성 평가
```

이는 방법론이 안정성을 유지하면서 학습하고 개선할 수 있게 합니다. 헌법은 날짜가 표시된 수정 사항과 함께 자체 진화를 보여주며, 실제 경험을 바탕으로 원칙이 어떻게 정제될 수 있는지 보여줍니다.

### 규칙을 넘어서: 개발 철학

헌법은 단순한 규칙집이 아닙니다. LLM이 코드 생성에 대해 생각하는 방식을 형성하는 철학입니다:

- **불투명함보다 관찰 가능성**: 모든 것이 CLI 인터페이스를 통해 검사 가능해야 함
- **영리함보다 단순함**: 단순하게 시작하고, 증명된 경우에만 복잡성 추가
- **격리보다 통합**: 인위적인 환경이 아닌 실제 환경에서 테스트
- **모놀리스보다 모듈성**: 모든 기능은 명확한 경계를 가진 라이브러리

이러한 원칙을 사양 및 계획 프로세스에 내장함으로써, SDD는 생성된 코드가 단순히 기능적인 것이 아니라 유지보수 가능하고, 테스트 가능하며, 아키텍처적으로 건전하도록 보장합니다. 헌법은 AI를 코드 생성기에서 시스템 설계 원칙을 존중하고 강화하는 아키텍처 파트너로 변환합니다.

## 변화 (The Transformation)

이것은 개발자를 대체하거나 창의성을 자동화하는 것이 아닙니다. 기계적인 번역을 자동화하여 인간의 능력을 증폭시키는 것입니다. 사양, 조사, 코드가 함께 진화하는 긴밀한 피드백 루프를 생성하여, 각 반복이 의도와 구현 사이의 더 깊은 이해와 더 나은 정렬을 가져오게 하는 것입니다.

소프트웨어 개발은 의도와 구현 사이의 정렬을 유지하기 위한 더 나은 도구가 필요합니다. SDD는 코드를 단순히 안내하는 것이 아니라 생성하는 실행 가능한 사양을 통해 이러한 정렬을 달성하는 방법론을 제공합니다.
